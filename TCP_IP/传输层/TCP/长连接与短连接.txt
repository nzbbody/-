/**
* @defgroup TCP
* @{
*/

/**
@verbatim
1、长连接和短连接的使用场景不一样：
	长连接建立连接后，开始频繁地交互。
	短连接建立连接后，交互一下子，下一次交互不知道什么时候。
2、tcp连接一旦长时间不交互，连接就会断掉。那么，对于长连接怎么保持连接不断呢？
	使用tcp的保活机制，keep-alive，
	当然也可以在应用层处理，定时发送心跳包。
@endverbatim
*/
长连接和短连接;


/** @} */


